# 第一卷《白色边缘》伏笔矩阵跨章回归

## 回归状态

- 日期：2026-08-17
- 范围：`03-结构化设定/连续性/伏笔矩阵.md` 与第一卷36章章节卡、场景卡审校、正文审校及 Canon Change
- 结果：矩阵中的51个正式伏笔 ID 均能在第一卷的章节设计或审校记录中找到落点；未发现新增正式 ID 未登记的当前问题
- 状态：通过；不改变正文，不新增 Canon

## 核对结果

### 已登记且有章节落点

早期的 `SSS-1`、`SSS-2`、`R-01`、`EDGE-01`、`EDGE-02`、`M-01` 等，已经通过第1—7章的不可见危险、边缘与任的身份异常建立；`SRC-01`、`REL-01`、`REL-02`、`CITY-01` 等在第8—10章进入社会与制度层；`M-02`、`M-03`、`ACAD-01`、`REN-03` 在第11—13章推动村庄高潮与学院介入。

第14—24章的 `S-01`、`GEO-01`、`SAIN-01`、`COLOR-01`、`LUMI-01`、`CONTOUR-01`、`REN-06`、`FIRE-01`、`BBOX-01`、`ARCH-01` 均有对应课堂、关系、火兽、范围或遗迹事件；第25—36章的 `E-01`、`VOID-01`、`FAIL-01`、`Z-08` 以及 `SSS-4` 的第一卷接口均保留在相应章节和审校记录中。

### 有意不进入伏笔矩阵的编号

- `CV-02`、`CV-04` 和 `ROI` 属于技术概念总登记表的翻译编号，不是独立的叙事伏笔 ID；它们的条件和失败边界已在技术登记表、章节卡与 Canon Change 中记录。
- `REN-07` 曾在第21章场景卡首轮审校中被发现为未登记候选，随后已删除并改为推进既有 `REN-06`。它不应补回矩阵。

### 未提前支付的长线问题

- `SSS-1`、`SSS-2`、`SSS-3`、`SSS-4` 仍然只提供错误模型和残缺接口，没有解释任的完整身份、唯一原图、被观察者或世界表示。
- `Z-01`、`Z-03`、`Z-04`、`Z-06`、`Z-08` 推进了零的可见性、被选择和行动代价，但没有把 Zero 写成 Alpha，也没有把零的关系问题收束成答案。
- `G-01`、`ARCH-01` 和 `VOID-01` 仍是漂亮的局部解释，不被升级为 Ground Truth、古代文明真相或世界本体。

## 回归结论

第一卷的伏笔账与章节账目前一致。后续如新增或修改伏笔，必须同时更新：章节卡、中央矩阵、Canon Change（若改变正式设定）和对应审校记录；不能只在正文或某一章作者侧说明中留下新编号。

下一次若发生第一卷正文修订，先重跑本回归，再判断是否需要修改收束审计。
